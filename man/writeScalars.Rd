% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ModelArray_Constructor.R
\name{writeScalars}
\alias{writeScalars}
\title{Write transformed element-by-subject data to the HDF5 scalars group.}
\usage{
writeScalars(
  fn.output,
  scalar_matrix,
  scalar_name,
  column_names = NULL,
  overwrite = TRUE,
  flush_every = 1000L,
  storage_mode = "double",
  compression_level = 4L
)
}
\arguments{
\item{fn.output}{A character path to the output HDF5 (.h5) file.}

\item{scalar_matrix}{Numeric/integer matrix (or data.frame coercible to matrix),
shaped as elements x subjects.}

\item{scalar_name}{A character scalar name used under \code{scalars/<scalar_name>}.}

\item{column_names}{Character vector of subject/source names. If NULL, uses
colnames(scalar_matrix).}

\item{overwrite}{If TRUE, overwrite existing \code{scalars/<scalar_name>} group.
If FALSE and it exists, emit warning and do nothing.}

\item{flush_every}{Positive integer number of rows (elements) written per block.}

\item{storage_mode}{Storage mode used by \code{rhdf5::h5createDataset}.
Typical values include \code{"double"} and \code{"integer"}.}

\item{compression_level}{Gzip compression level passed to
\code{rhdf5::h5createDataset} (0-9).}
}
\description{
Create (or overwrite) \code{scalars/<scalar_name>/values} in an HDF5 file and
write data in row blocks to limit peak memory use for large matrices.
}
\details{
\code{scalar_matrix} is expected to be shaped as \strong{elements x subjects}.
\code{column_names} must correspond to subjects/sources and is saved both as:
\enumerate{
\item attribute \code{column_names} on \code{scalars/<scalar_name>/values}, and
\item dataset \code{scalars/<scalar_name>/column_names}
}
for compatibility with existing ModelArray readers.
}
